spring:
  application:
    name: suntion-hystrix
  profiles:
    active: ${active.profile}
  cloud:
    config:
      uri: ${configServer.url}
      label: master
    bootstrap:
      enabled: true
logging:
  path: /var/log/${spring.application.name}
