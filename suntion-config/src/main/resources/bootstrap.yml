server:
  port: 8888
  ssl:
    key-store: classpath:suntion.cc.pfx
    key-store-type: PKCS12
    key-store-password: cgTv99ha
spring:
  application:
    name: suntion-config
  profiles:
    active: ${active.profile}
  cloud:
    config:
      server:
        git:
          uri: https://github.com/suntion/suntion-springcloud-config.git
          searchPaths:
            - third-env/dev
            - third-env/test
            - third-env/prod
