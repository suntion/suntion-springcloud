spring:
  application:
    name: suntion-gateway
  profiles:
    active: ${active.profile}
  cloud:
    config:
      label: ${config.label}
      discovery:
        enabled: true
        service-id: ${eureka.serviceId}
eureka:
  client:
    serviceUrl:
      defaultZone: ${eureka.address}
