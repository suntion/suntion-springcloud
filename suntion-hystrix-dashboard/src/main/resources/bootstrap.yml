spring:
  application:
    name: suntion-hystrix-dashboard
  profiles:
    active: ${active.profile}
  cloud:
    config:
      label: ${config.label}
      discovery:
        enabled: true
        service-id: ${eureka.serviceId}
eureka:
  client:
    serviceUrl:
      defaultZone: ${eureka.address}
